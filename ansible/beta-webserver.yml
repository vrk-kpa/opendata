---

- hosts: beta-webserver
  accelerate: false
  sudo: true
  roles:
    - ansible
    - firewall
    - users
  vars_files:
    - "vars/common.yml"
    - ["vars/local.yml", "vars/none.yml"]

- hosts: beta-webserver
  accelerate: "{{ accelerate }}"
  sudo: true
  vars_files:
    - "vars/common.yml"
    - ["vars/private/{{ private_variables }}", "vars/none.yml"]
    - ["vars/local.yml", "vars/none.yml"]
  vars:
    accelerate: false
  roles:
    - clean
    - common
    - common_webserver
    - nginx
    - postfix
    - jetty
    - ckan
    - drupal
    - data
