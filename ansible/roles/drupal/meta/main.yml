---
galaxy_info:
  description: Drupal for Ubuntu

  license: MIT

  min_ansible_version: 2.6

  platform:
    - name: Ubuntu
      versions:
        - xenial
        - bionic

  galaxy_tags:
    - php
    - development
    - web
    - drupal

dependencies:
  - php
    tags: dependency_role
  - composer
    tags: dependency_role
  - opendata_frontend
    tags: dependency_role
